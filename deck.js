export default Deck;

function Deck ({}) {
    this.cards = [];
    
}
Deck.prototype.createDeck = function () {
    // creates the deck
};

const deck = new Deck(); //creates instance of deck
deck.createDeck()