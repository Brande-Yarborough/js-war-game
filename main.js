import game from './game.js';
import deck from './deck.js';
import card from './card.js';
import player from './player.js';










//create new deck of cards with all 52 cards, one for each suit and value combination
//loop through all suits, loop through all values, and combine togehter in one array
// function freshDeck () {
// return suits.flatMap(suit => { //loop through each one of suits. condenses nested arrays into one array
// return values.map(value => { //loop through values and map into array
//     return new Card(suit, value)
//     })
//    })
// }

// console.log(deck.cards);





















